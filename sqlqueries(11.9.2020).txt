1)
SELECT FNAME,CITY,COUNTRY
FROM CUSTOMER
WHERE CITY in ( 'Chennai', 'Salem', 'Madurai');

2)
SELECT CUSTOMER.FNAME,CUSTOMER.LNAME,CUSTOMER.DATEFIRSTPURCHASED,ITEMS.ITEMNAME,ITEMS.PRICE 
from SAMPLE.CUSTOMER, SAMPLE.ITEMS WHERE CUSTOMER.SUPPLIERNO=ITEMS.SUPPLIERID;

3)
SELECT * from SAMPLE.ORDERS where ITEMID = all (select ITEMID from SAMPLE.ITEMS where ITEMNAME='Dove Soap');